# Copy to .env (git-ignored) and fill in. `make` loads these when they
# aren't already exported. Only used when no Codex / Claude Code subscription
# is logged in on this machine; OpenAI wins if both are set.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
